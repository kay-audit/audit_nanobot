# Скрипт «Невозможность взыскания задолженности по кредитному продукту»

> **Notebook:** [`notebooks/credit_no_way_collect_debt_v2.ipynb`](../notebooks/credit_no_way_collect_debt_v2.ipynb)
> **Skill ID:** `credit_no_way_collect_debt_v2`
> **Категория:** расчёт финансовых последствий по кредитному продукту

> **Статус:** полная расчётная логика (ФЛ / ЮЛ ММБ / ЮЛ КСБ). I/O вынесен
> в блок `PARAMS` (без интерактивного ввода). Формулы расчёта РВПС/залога/
> коэффициентов на 46 типов отклонений сохранены без изменений. Требует
> проверки на реальных кредитных витринах перед боевым использованием.

> Единый источник логики — `scripts/credit_calc_source.py` (проходит
> py_compile); ноутбук собирается из него `scripts/build_credit_notebook.py`.

---

## 1. Краткое описание для LLM-маршрутизатора

Рассчитывает **финансовые последствия (прямые/косвенные/потенциальные
потери)** по конкретному кредитному продукту в случае невозможности
взыскания задолженности. Покрывает 4 основания (п.3 Таблицы 9):

- пропущен срок предъявления требований;
- истёк срок исковой давности;
- заёмщик ликвидирован;
- невключение/несвоевременное включение в РТК.

**Когда LLM выбирает:** запрос про **расчёт потерь по кредиту, РВПС,
невозможность взыскания, списание задолженности, залог** с указанием
ID кредита и параметров. Это НЕ ИОР-отчёт — это калькулятор по одному
кредиту.

## 2. Источники (кредитные витрины — НЕ в основной БЗ ИОР)

| Таблица | Назначение |
|---------|------------|
| `d6_base_of_knowledge_b2c_credit_fl` | кредиты физлиц |
| `d6_base_of_knowledge_cred_yul_new_corr` | кредиты юрлиц (фильтр по `host_agr_cred_id`; есть и залог `obj_plg_*`) |
| `d6_base_of_knowledge_custom_risk_kfl_v_relevant_loans_saldo` | резерв (РВПС) по датам |
| `d6_base_of_knowledge_cards` | кредитные карты ФЛ |
| `d6_base_of_knowledge_risk_parsed_rsk_reqst` | заявки на КК |

## 3. Параметры (вместо интерактивного ввода)

```python
PARAMS = {
    "client_type": "ФЛ",           # ФЛ | КСБ | ММБ
    "id_credit": "52802479370431", # ID кредита / номер договора КК
    "incident_date": "2026-01-22", # дата обнаружения инцидента ГГГГ-ММ-ДД
    "risk_profile_code": "DRP-10047", # код ЦПР (см. digital_risk_profile)
    "deviation_code": "44",        # код отклонения (см. deviation_type)
    "factor_codes": [],            # факторы ОР (для DRP-10023/др., 1-4 шт.)
    "drp_10027_type": None,        # 1=Утрата | 2=Неоформление (для DRP-10027)
    "zalog_overact_amount": None,  # сумма завышения залога (если требуется)
    "vivod_sredstv_pct": None,     # % вывода средств (DRP-10023)
    "vivod_sredstv_amount": None,  # сумма вывода средств (DRP-10023)
}
```

## 4. Параметризация I/O и расчётные ветки

| Интерактивный ввод | Параметр |
|----------------------|----------|
| выбор клиента/кредита/ЦПР/отклонения | `PARAMS` (client_type, id_credit, ...) |
| выбор факторов ОР | `PARAMS["factor_codes"]` |
| сумма залога/вывода средств | `PARAMS["zalog_overact_amount"]` и т.д. |
| вид события DRP-10027 (утрата/неоформление) | `PARAMS["drp_10027_type"]` |

**Расчётные функции:**
- `physical_face_calculation` — ФЛ
- `juridical_face_calculation` — ЮЛ сегмент ММБ
- `juridical_face_ksb_calculation` — ЮЛ сегмент КСБ

**Корректировки данных по ЮЛ:**
- `get_zalog_jur_data` — залог запрашивается напрямую из `cred_yul_new_corr`
  (obj_plg_sid, OBJ_PLG_COLLATERAL_COST_UPD_RUB_AMT, agr_cred_id) по
  `host_agr_cred_id` + `gregor_dt`. (Таблицы `cred_yul` нет; `cred_yul_new_corr` —
  единственная, где есть и `host_agr_cred_id`, и колонки залога.)
- `get_loan_juridical_face_data` — из `cred_yul_new_corr`, без фильтра `src_sys_id`
  (его в этой таблице нет; он есть только в `cred_yul_old`, где нет колонок залога).
- В КСБ-функциях порядок получения данных на дату выдачи перенесён до их
  использования (устранён доступ к переменной до присвоения);
  `.isDigit()` → `.isdigit()`.

## 5. Триггеры

| Триггер | Распознать |
|---------|------------|
| «рассчитай потери по кредиту {id}» | основная тема |
| «невозможность взыскания», «РВПС», «списание долга» | расчёт |
| «потери по залогу», «утрата обеспечения» | DRP-10027 |
| «ВЫВОД СРЕДСТВ ЗАЁМЩИКОМ» | DRP-10023 |

```json
{
  "skill_id": "credit_no_way_collect_debt_v2",
  "type": "report",
  "category": "credit_loss_calc",
  "status": "draft",
  "params_schema": {
    "client_type": "string (ФЛ|КСБ|ММБ)",
    "id_credit": "string",
    "incident_date": "string (ГГГГ-ММ-ДД)",
    "risk_profile_code": "string (DRP-XXXXX)",
    "deviation_code": "string"
  }
}
```

## 6. Завершение

`spark.stop()` в конце. Результат — расчёт потерь (прямые/косвенные/
потенциальные) в выводе ячейки + при необходимости выгрузка в Excel.
