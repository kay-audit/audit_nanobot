# feature.yaml - обязательный манифест ветки feature/d6-nanobot-with-manifest.
#
# Описывает содержимое ветки d6_nanobot для apply через audit_bridge:
# - skill ior-analyzer (паттерн full, регистрация native-tool);
# - tool ior_analyzer (config_key=ior_analyzer, native-tool registration);
# - cross-cutting файл lib/services/skill_runtime_mode.py (используется
#   обоими: этим tool и sql_assistant_runtime);
# - testing-инфраструктура:
#     * filesystem-cache (data_generator → ior.json) — primary для pre-flight;
#     * PostgreSQL variant (V005 migration + pg_loader.py → test_d6.ior_events)
#       — для SQL-flow (skill читает напрямую через workspace.utils.db).
#
# Все пути относительно корня audit_nanobot.

feature:
  id: d6_nanobot
  name: ior-analyzer
  version: 1.0.0
  description: >
    Анализ инцидентов операционного риска (ИОР): стандартные выгрузки,
    ad-hoc запросы, отчёты, гипотезы и уточнения по выгрузке.
  documentation: docs/D6.md
  maintainer: "@kay-audit/audit-team"

requires:
  nanobot_version: ">=2.5.0"
  system_deps:
    - python>=3.14
  env_vars:
    - name: NANOBOT_SKILLS_RUNTIME
      required: false
      default: production
      allowed: [production, testing]
      description: "Режим работы skill'ов (production/testing)"
    - name: IOR_RAG_CACHE_DIR
      required: false
      default: cache_final
      description: "Имя директории кеша BGE-M3 retrieval (относительно PIPELINES_CACHE_DIR)"
    - name: PIPELINES_CACHE_DIR
      required: false
      default: ""
      description: "Корень кешей pipelines; если пусто - автодеривация"
    - name: EMBED_TOKEN
      required: false
      scope: cross_cutting
      description: "Bearer-токен для Ollama /api/embed reverse proxy (cross-cutting)"
    - name: IOR_DATA_BACKEND
      required: false
      default: cache
      allowed: [cache, local_duckdb, greenplum, spark]
      description: >
        Бэкенд data_store.py для чтения production-данных.
        Без явного override выбор делается по NANOBOT_SKILLS_RUNTIME:
          testing    → cache     (NanobotCacheStore → DuckDB-кэш из локальной PG)
          production → greenplum (GreenplumStore → прямой SQL в GP).
  secrets:
    - name: DATABASE_URL
      source: existing
    - name: OPENAI_API_KEY
      source: existing
    - name: IOR_TEST_DSN
      source: optional            # переопределяет channels.postgres.dsn для pg_loader
  sql_migrations: []

skills:
  - name: ior-analyzer
    path: workspace/skills/ior-analyzer
    pattern: full
    # Регистрируется в project.json::skills.ior-analyzer (см. overlays.project_json):
    #   * tables[] — синхронизация в DuckDB-кэш через PgDuckDbSyncService;
    #   * без cli.*/llm.*/vector_indexes — tool-only skill.
    config_section: skills.ior-analyzer

tools:
  - name: ior_analyzer
    path: workspace/tools/ior_analyzer.py
    config_key: ior_analyzer
    config_section: gateway.ior_analyzer

overlays:
  project_json:
    - op: upsert
      path: gateway.ior_analyzer.enable
      value: true
      rationale: >
        Регистрация native tool через RuntimePatcher.patch_project_tools.
        config_key="ior_analyzer", секция gateway.ior_analyzer обязательна
        даже при наличии safety-дефолта в tool'е (явная запись в git diff -
        единственный source of truth для аудита).
    - op: upsert
      path: skills.ior-analyzer.enabled
      value: true
      rationale: >
        Регистрация доменных таблиц IOR для синхронизации в DuckDB-кэш.
        Без этой записи PgDuckDbSyncService не подхватывает таблицы, и
        skill получает "схема не существует" при попытке чтения через
        NanobotCacheStore. На DEV таблицы лежат в public (после миграции
        V006 и загрузки pg_loader_real.py); на ПРОМ — в GP, отдельный
        project.json. Выбор backend'а (cache vs greenplum) делается
        runtime-aware в data_store.py по NANOBOT_SKILLS_RUNTIME.
    - op: upsert
      path: skills.ior-analyzer.tables
      value:
        - name: public.t_db_oarb_ior_d6_base_of_knowledge_ior
          tracking_column: incdnt_id
        - name: public.t_db_oarb_ior_d6_base_of_knowledge_incident_stts_chng
          tracking_column: incdnt_id
        - name: public.t_db_oarb_ior_d6_base_of_knowledge_incident_recovery
          tracking_column: incdnt_id
        - name: public.t_db_oarb_ior_d6_base_of_knowledge_incident_fin_impact
          tracking_column: incdnt_id
        - name: public.t_db_oarb_ior_d6_base_of_knowledge_incident_nonfin_impact
          tracking_column: incdnt_id
      rationale: >
        tracking_column=incdnt_id — PK основной таблицы (монотонный BIGINT,
        совместим с инкрементальным sync'ом). Связанные таблицы (status/
        recovery/fin/nonfin) тоже используют incdnt_id как ссылку на основную
        таблицу, поэтому один tracking_column для всех 5 — корректен.
        Sync подхватывает только эти 5 таблиц; DuckDB-кэш хранит их под
        bare-именами (без схемы public).
  env_files: []
  templates: []
  sql_migrations:
    # V005 создаёт изолированную test_d6 schema с таблицей ior_events.
    # Применяется через `python tools/migrate.py --apply` ПОСЛЕ apply фичи
    # (applier копирует файл, но НЕ выполняет DDL — это сознательное
    # разделение, см. docs/feature-bridge-authoring.md §3.5).
    # DROP CASCADE при rollback фичи (см. isolation.removable=true).
    - sql/migrations/V005__d6_ior_test_schema.sql
    # V006 создаёт 5 production-таблиц IOR (t_db_oarb_ior_d6_*) под схемой
    # public в локальной PostgreSQL. Имена — production-реальные (совпадают
    # с ПРОМ GP), чтобы DuckDB-кэш мог переиспользовать ту же раскладку
    # колонок. На ПРОМ таблицы уже существуют (DDL не нужен), миграция
    # применяется только в DEV-окружении через applier (pg_loader_real.py
    # заполняет таблицы тестовыми данными). Источник данных выбирается
    # runtime-aware в data_store.py по NANOBOT_SKILLS_RUNTIME.
    - sql/migrations/V006__d6_ior_tables_real.sql

lib_additions:
  - path: lib/services/skill_runtime_mode.py
    justification: >
      Cross-skill runtime switch. Импортируется workspace/tools/ior_analyzer.py
      (tool layer, до загрузки skill dir на sys.path) и
      lib/services/sql_assistant_runtime.py. Не может жить внутри skill'а,
      потому что tool не должен импортировать workspace/skills/* (Skill↔Tool
      инвариант из docs/TARGET_ARCHITECTURE.md §22).
    scope: cross_cutting
    reuse_targets:
      - workspace/tools/ior_analyzer.py
      - lib/services/sql_assistant_runtime.py

tests:
  - path: tests/test_tools_ior_analyzer.py
    marker: optional        # LLM-зависимости мокаются; red на CI не блокирует apply
  - path: tests/test_ior_bge_followup_contract.py
    marker: required        # контракт BGE-M3 retrieval - критичен для прод-логики

testing:
  runtime_env: NANOBOT_SKILLS_RUNTIME
  runtime_value: testing
  runtime_data_backend: testing_pg   # целевое состояние по PRT8
  data_generator:
    path: workspace/skills/ior-analyzer/testing/data_generator.py
    command: "python workspace/skills/ior-analyzer/testing/data_generator.py --force"
    # Генерирует JSON-файл для filesystem-cache (быстро, детерминизм).
    target_storage: filesystem
    filesystem:
      target_path: workspace/data_store/cache/testing/ior/ior.json
    idempotent: true
    deterministic: true
    seed: 20260921
    record_count: 1000
  pg_loader:
    # Дополнительно: заливает тот же JSON в PostgreSQL (test_d6.ior_events).
    # Это даёт skill'у возможность читать тестовые данные через SQL-flow
    # (workspace.utils.db) для аудита, ad-hoc запросов, отчётов.
    # Запускается ПОСЛЕ data_generator (использует тот же JSON).
    # Требует: V005 миграция применена + $IOR_TEST_DSN или channels.postgres.dsn.
    # Тот же JSON может быть залит в production-таблицы IOR (public.t_db_oarb_ior_d6_*)
    # скриптом workspace/skills/ior-analyzer/testing/pg_loader_real.py после V006.
    path: workspace/skills/ior-analyzer/testing/pg_loader.py
    command: "python workspace/skills/ior-analyzer/testing/pg_loader.py --json workspace/data_store/cache/testing/ior/ior.json --schema test_d6 --table ior_events"
    requires_migration: V005__d6_ior_test_schema
    idempotent: true   # TRUNCATE + bulk INSERT в одной транзакции
  runner:
    path: workspace/skills/ior-analyzer/testing/runner.py
    smoke_command: "python workspace/skills/ior-analyzer/testing/runner.py --smoke"
    smoke_prompt: "сколько инцидентов в 2024 году?"
    smoke_expected:
      contains_section: "Финансовые потери"
      min_records_in_evidence: 1
  pytest:
    command: "python -m pytest tests/test_tools_ior_analyzer.py tests/test_ior_bge_followup_contract.py -q --tb=short -p no:cacheprovider"
    marker_required_strict: true
    marker_optional_strict: false

isolation:
  removable: true
  strategy: files_and_config
  # Полный список файлов из git diff origin/master..origin/d6_nanobot.
  # Глоб workspace/skills/ior-analyzer/** покрывает все 89 файлов под этим
  # префиксом (knowledge_base, scripts, testing, utils, data_store).
  # Остальные файлы (документация, tool, тесты, миграция, pg_loader) перечислены явно.
  files_added:
    - lib/services/skill_runtime_mode.py
    - lib/core/skill_config.py
    - docs/D6.md
    - docs/README.md
    - docs/feature-bridge-authoring.md
    - .gitignore
    - workspace/skills/ior-analyzer/**
    - workspace/tools/ior_analyzer.py
    - sql/migrations/V005__d6_ior_test_schema.sql
    - sql/migrations/V006__d6_ior_tables_real.sql
    - workspace/skills/ior-analyzer/testing/pg_loader_real.py
    - tests/test_tools_ior_analyzer.py
    - tests/test_ior_bge_followup_contract.py
  files_modified:
    - project.json        # + skills.ior-analyzer (таблицы IOR для sync)
    - feature.yaml        # + pg_loader_real, V006, project_json overlay
    - workspace/skills/ior-analyzer/utils/data_store.py  # runtime-aware backend (testing→cache, production→greenplum)
    - workspace/tools/ior_analyzer.py                    # убрана JSON-ветка; testing идёт через ior_reports с другим backend
  config_keys_added:
    - gateway.ior_analyzer.enable
    - skills.ior_analyzer.enabled
    - skills.ior_analyzer.tables
  config_keys_removed: []
